'use strict';

const AWSListenerTest = require('./aws-listener-test');
const AWSListenerTestError = require('./aws-listener-test-error');
const AWSExampleEvents = require('./events/');

module.exports = {
	AWSListenerTest,
	AWSListenerTestError,
	AWSExampleEvents
};
